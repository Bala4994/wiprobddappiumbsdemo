To run android -copy app id inside browserstackandr.yml to browserstack.yml and update userkey,accesskey & bsid

To run ios -copy app id inside browserstackios.yml to browserstack.yml and update userkey,accesskey & bsid
